schema_version: 1

context:
  version: "0.2.0.9000"
  rev: 8c0d3f0572da308c137adbaa262f5a2d17b52021

package:
  name: r-anicheck
  version: ${{ version }}

source:
  git: https://github.com/animovement/anicheck.git
  rev: ${{ rev }}

build:
  number: 1
  noarch: generic
  script:
    # Drop dev-only renv files from the git checkout; otherwise the
    # project .Rprofile activates renv and hijacks the library path.
    - rm -f .Rprofile renv.lock
    - rm -rf renv
    - R CMD INSTALL --no-multiarch --with-keep.source .

requirements:
  host:
    - r-base >=4.1.0
    - r-aniframe
    - r-cli
    - r-rlang
  run:
    - r-base >=4.1.0
    - r-aniframe
    - r-cli
    - r-rlang

tests:
  - script:
      - Rscript -e "library(anicheck)"

about:
  homepage: https://github.com/animovement/anicheck
  summary: An R package for diagnosing movement data quality
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - roaldarbol
