schema_version: 1

context:
  version: "0.5.0.9002"
  rev: 9111511672e8784a9b9b52bf5ff0b665034d2ea7

package:
  name: r-aniprocess
  version: ${{ version }}

source:
  git: https://github.com/animovement/aniprocess.git
  rev: ${{ rev }}

build:
  number: 0
  noarch: generic
  script:
    # Drop dev-only renv files from the git checkout; otherwise the
    # project .Rprofile activates renv and hijacks the library path.
    - rm -f .Rprofile renv.lock
    - rm -rf renv
    - R CMD INSTALL --no-multiarch --with-keep.source .

requirements:
  host:
    - r-base >=4.1.0
    - r-anicore >=0.8.0
    - r-cli
    - r-data.table >=1.18.0
    - r-dplyr >=1.1.0
    - r-lifecycle
    - r-rlang
    - r-tidyselect
  run:
    - r-base >=4.1.0
    - r-anicore >=0.8.0
    - r-cli
    - r-data.table >=1.18.0
    - r-dplyr >=1.1.0
    - r-lifecycle
    - r-rlang
    - r-tidyselect

tests:
  - script:
      - Rscript -e "library(aniprocess)"

about:
  homepage: https://github.com/animovement/aniprocess
  summary: An R package for signal processing and filtering of movement data
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - roaldarbol
