schema_version: 1

context:
  version: "0.5.0.9000"
  rev: 2c3244196fa8447c35fb2597b97ff43b9dcd4a00

package:
  name: r-animetric
  version: ${{ version }}

source:
  git: https://github.com/animovement/animetric.git
  rev: ${{ rev }}

build:
  number: 0
  noarch: generic
  script:
    # Drop dev-only renv files from the git checkout; otherwise the
    # project .Rprofile activates renv and hijacks the library path.
    - rm -f .Rprofile renv.lock
    - rm -rf renv
    - R CMD INSTALL --no-multiarch --with-keep.source .

requirements:
  host:
    - r-base >=4.1.0
    - r-anicore >=0.8.0
    - r-anispace
    - r-cli
    - r-dplyr
    - r-purrr
    - r-rlang
  run:
    - r-base >=4.1.0
    - r-anicore >=0.8.0
    - r-anispace
    - r-cli
    - r-dplyr
    - r-purrr
    - r-rlang

tests:
  - script:
      - Rscript -e "library(animetric)"

about:
  homepage: https://github.com/animovement/animetric
  summary: An R package for calculating movement-based metrics
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - roaldarbol
