schema_version: 1

context:
  version: "0.3.2.9000"
  rev: 4a3b89bd2597e5c549e4c36b564aedb5a0a0cd54

package:
  name: r-animetric
  version: ${{ version }}

source:
  git: https://github.com/animovement/animetric.git
  rev: ${{ rev }}

build:
  number: 0
  noarch: generic
  script:
    # Drop dev-only renv files from the git checkout; otherwise the
    # project .Rprofile activates renv and hijacks the library path.
    - rm -f .Rprofile renv.lock
    - rm -rf renv
    - R CMD INSTALL --no-multiarch --with-keep.source .

requirements:
  host:
    - r-base >=4.1.0
    - r-aniframe
    - r-anispace
    - r-cli
    - r-dplyr
    - r-purrr
    - r-rlang
  run:
    - r-base >=4.1.0
    - r-aniframe
    - r-anispace
    - r-cli
    - r-dplyr
    - r-purrr
    - r-rlang

tests:
  - script:
      - Rscript -e "library(animetric)"

about:
  homepage: https://github.com/animovement/animetric
  summary: An R package for calculating movement-based metrics
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - roaldarbol
