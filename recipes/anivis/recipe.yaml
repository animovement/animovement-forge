schema_version: 1

context:
  version: "0.2.0"
  rev: f9c1e737abe0a9383ba65838cf385e40eb96e41c

package:
  name: r-anivis
  version: ${{ version }}

source:
  git: https://github.com/animovement/anivis.git
  rev: ${{ rev }}

build:
  number: 0
  noarch: generic
  script:
    # Drop dev-only renv files from the git checkout; otherwise the
    # project .Rprofile activates renv and hijacks the library path.
    - rm -f .Rprofile renv.lock
    - rm -rf renv
    - R CMD INSTALL --no-multiarch --with-keep.source .

requirements:
  host:
    - r-base
    - r-rlang
    - r-ggplot2
    - r-patchwork
    - r-marquee
  run:
    - r-base
    - r-rlang
    - r-ggplot2
    - r-patchwork
    - r-marquee

tests:
  - script:
      - Rscript -e "library(anivis)"

about:
  homepage: https://github.com/animovement/anivis
  summary: An R package for visualizing movement data and diagnostics
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - roaldarbol
