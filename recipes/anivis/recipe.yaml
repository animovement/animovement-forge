schema_version: 1

context:
  version: "0.2.0"
  rev: 36cc865882176308b92a467c22bf238f52df5e2f

package:
  name: r-anivis
  version: ${{ version }}

source:
  git: https://github.com/animovement/anivis.git
  rev: ${{ rev }}

build:
  number: 1
  noarch: generic
  script:
    # Drop dev-only renv files from the git checkout; otherwise the
    # project .Rprofile activates renv and hijacks the library path.
    - rm -f .Rprofile renv.lock
    - rm -rf renv
    - R CMD INSTALL --no-multiarch --with-keep.source .

requirements:
  host:
    - r-base
    - r-rlang
    - r-ggplot2
    - r-patchwork
    - r-marquee
    - r-hms
    - r-cli
    - r-aniframe >=0.6.0
  run:
    - r-base
    - r-rlang
    - r-ggplot2
    - r-patchwork
    - r-marquee
    - r-hms
    - r-cli
    - r-aniframe >=0.6.0

tests:
  - script:
      - Rscript -e "library(anivis)"

about:
  homepage: https://github.com/animovement/anivis
  summary: An R package for visualizing movement data and diagnostics
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - roaldarbol
